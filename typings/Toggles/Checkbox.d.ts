export declare const Checkbox: ({ checked, disabled, handleChange, id, label, name, required, ...props }: {
    checked?: boolean;
    disabled?: boolean;
    handleChange: () => void;
    id: string;
    label: string;
    name: string;
    required?: boolean;
}) => any;
